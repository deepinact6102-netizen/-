# プロジェクト史（2026-08-01 時点の発掘結果）

このファイルは、**git の履歴からしか復元できない情報**を残すためのもの。
セッションは互いに隔離されていて会話は読めない。読めるのはコミット・差分・ブランチだけ。
同じ発掘を次のセッションが繰り返さないよう、判明したことをここに集約する。

日々の運用ルールと未対応の課題は `CLAUDE.md` を見ること。

> 表記: **【事実】** = コミットや差分が直接証明していること /
> **【推測】** = そこから読み取れるが証明はできないこと。

---

## 1. プロダクトの正体

**実在する焼き鳥店の店主向けに作られた、発注数の自動計算アプリ。**
単一 HTML に JS を全部インラインで書いた構成。ビルド工程・テスト・lint はいずれも無い。
mycomise.com として Vercel で配信され、Stripe の月額サブスクで販売されている。

### 業務モデル【事実】

- 基本9品目（ねぎま／むね／ぼんじり／せせり／ハツ／レバー／とり皮／砂肝／ささみ）＋ 自由枠2つ
- **納品は中1日**（当日の営業終了後に発注 → 翌々日納品）。日曜は業者休み
- **焼き鳥の発注は5本単位で切り上げ**（`ceilTo5`）。原料はパック単位で切り上げ（`ceilToPack`）
- コース対象は **ねぎま・ハツ・ささみ** の3品。コース人数分が予想に加算される
- 予想 ＝ `(1日目安 × 注文日数 ＋ コース人数) − (残数 ＋ 翌日納品 − 1日目安)`、下限0
- データは全て localStorage。**サーバー同期もバックアップも無い**（端末を変えると引き継げない）

### 店の規模【事実 → 推測】

初日の1日目安は合計560本だったが、**27分後に185本へ全面差し替えられている**（`a5d1f63`）。
この185本が現在も既定値。**【推測】** 560本は当てずっぽうで、185本が実際の店の数字。
個人経営規模と見てよい。ねぎま・ハツ・ささみが各30本で最多、むね・レバーが10本で最少。

### 使われ方【推測】

- 営業終了後に**紙へ手書き** → 翌朝それを**撮影して OCR** という運用（`print.html` ＋ 説明書）
- 記録用紙が3×3の9枚 → 約9営業日分をまとめて印刷して貼っておく想定
- 途中で全体フォントを18pxに拡大している（`55bc006`）
- 説明書に「明るい店内では白、暗めの店内では黒が見やすい」→ 厨房と客席で明るさが違う

---

## 2. セッション年表

コミット本文に埋め込まれた `claude.ai/code/session_*` ID と、タイムスタンプの塊から復元。

| # | 時期 | やったこと | 現在 |
|---|---|---|---|
| S1 | 04-23 | 英語のテイクアウト注文 React アプリ `yakitori_app.jsx`（254行） | ❌ 死骸。08-01 に削除済み |
| S2 | 04-23 14:11（**2分**） | S1 を捨て、週次の発注管理コンポーネントを構築 | ⚠️ 設計は本流が再実装。**OCR 設計だけ失われた** |
| S3 | 04-23 15:28 | S2 と**バイト単位で完全に同一**のものを再実装 | ❌ 価値ゼロ |
| S4 | 04-30〜07-21（実作業**16日**・107コミット） | **製品のほぼ全て** | ✅ これが製品 |
| S5 | 05-05 | S1（5日前に破棄済み）の UI を日本語化 | ❌ 死んだファイルの翻訳 |
| S6 | 08-01 | セキュリティ修正・データ隔離・死蔵整理・本調査 | ✅ 本流へマージ済み |

### 3つのセッションが無駄になった原因【事実】

**GitHub の既定ブランチが4月のコミット（`claude/yakitori-app-component-SoIDX`）を指したまま。**
新しいセッションは毎回そこから始まるので、直前の成果が見えない。結果として —

- **S3 は S2 の75分後に、同じものをゼロから書き直した。** しかも S2 が既に直していた欠陥
  （自己相殺する数式項、未使用関数）をそのまま含んでいる
- **S5 は既に破棄済みのファイルを翻訳した**
- **S2 は正しい計算式に一発で到達していた。** その9日後、S4 は同じ結論に
  **84分・12回の書き直し**をかけて再到達している（`1cf60fd` → `948e503`）

**着手前に必ず `git merge-base` で本体との関係を確認すること。**
既定ブランチの変更はユーザー操作が必要（`Settings → General → Default branch`）。

---

## 3. 開発フェーズ

| 期間 | 内容 | コミット |
|---|---|---|
| 04-23 | 誤爆した別プロダクト（英語の注文アプリ） | 1 |
| 04-30 | 発注表の骨格を1日で構築。PWA 化、OCR、中1日リードタイム | 12 |
| 05-01夜〜05-02未明 | **計算式の迷走**。一晩で12回以上書き換え | 28 |
| 05-24〜05-26 | **商品化の下地**。店舗名・品目名・目安を全部ユーザー設定可能に | 19 |
| 05-27〜05-29 | 売り物としての体裁。説明書・アイコン・テーマ切替 | 17 |
| 06-30〜07-11 | 現場フィードバックの反映。記録用紙、注文日数「なし」 | 23 |
| 07-11〜07-21 | **有料サブスク化**。認証・Stripe・デモ版 | 7 |

**【推測】** 課金コード（7月）より**7週間早く**マルチテナント化（5月下旬）が済んでいる。
「自分の店で使えたから売れる」と考えて商品の形を先に整え、目処が立った7月に一気に課金配管を敷いた。
デモ版が課金実装の1週間後に来ているのは、ゲートを付けてから「試せない＝売れない」と気づいた順序。

---

## 4. 作って消したもの（最も情報量の多い記録）

| 消した内容 | 生存期間 | 学び |
|---|---|---|
| 前日発注→翌日納品 | **7分** | 中1日は動かせない業務制約 |
| レバー10本固定 | 2か月 | 自店特例は商品化の邪魔 |
| カレンダーのモーダル | 8分 | モーダルではなく独立ページが正解 |
| 日曜固定定休 | 3週間 | **定休日をコードに焼くのをやめた＝他店へ売る前提への転換点** |
| 原料の5本丸め | 2か月 | 焼き鳥のルールを原料に流用したのは誤り。パック単位へ |
| Service Worker | 32分 | キャッシュ戦略を諦め、以後「ファイル名を変える」戦法へ |
| コース人数の日付移動時リセット | **7分** | 日付ごとに記憶する方式へ反転 |

**7分で反転した仕様が2件ある。【推測】** 仕様を書き留めた場所がどこにも無く、毎回口頭で決めていた。
説明書は既に実装と3か所ずれている。**現行 v5.html の挙動を仕様として書き起こす価値がある。**

---

## 5. バージョンファイルが増えた経緯【事実】

```
yakitori_order.html [04-30] ─┬→ yakitori_order_v2.html [05-24]
                             └→ v2.html [05-24] ─┬→ v3.html [05-26]
                                                 └→ v4.html [05-26] → v5.html [05-27] → demo.html [07-18]
```

PWA 化で Service Worker のキャッシュを導入したが、更新が反映されない問題に苦しみ、
`CACHE = 'yakitori-vN'` を v2→v7 まで手で上げても解決せず、
**最終的に「ファイル名ごと変える」戦法に転換した**のが版が増えた理由（`fd5728f` が明言）。
その後 SW は unregister する方針になったが、**古いファイルは1つも削除されないまま残っていた。**

**08-01 に死蔵7ファイル（v2/v3/v4/yakitori_order×2/yakitori_app.jsx/sw.js）を削除済み。**
削除理由には保安上の問題もあった → 次節。

---

## 6. 失われた資産・回収したもの

### 回収済み

- **旧版の認証素通り**: `v2/v3/v4/yakitori_order*.html` には**認証ゲートが無い**。
  Vercel は静的ファイルをそのまま配信するため、ファイル名を直打ちすれば有料版とほぼ同等のものが
  無料で使えた。**08-01 の死蔵ファイル削除で解消。**
- **gh-pages 経由の認証素通り**: GitHub Pages には API を置けないため `/api/verify-code` が404 →
  旧コードのフェイルオープンで誰でも入れた。**08-01 に修正版を gh-pages へ反映して塞いだ**（`5cb2dc3`）。
  恒久対応は Pages 自体の停止（ユーザー操作）。

### まだ失われたまま

- **S2 の OCR 設計**（`mx11T` ブランチ `1ee1a53`、`yakitori_app.jsx:166-206`）。
  品目名をキーにした JSON で受け取り、読めない品目は null にする方式。
  **原理的に行ズレが起きない。** 現行の Tesseract 実装は位置で割り当てるため、
  1品目読み落とすと以降が全部ズレる（08-01 に名前ベース照合へ改修）。
  Claude Vision を使う元の設計に戻すならバックエンド側にプロキシが要る。

---

## 7. gh-pages の正体【事実】

別セッションの成果ではない。**本流と同一セッションが、コミットのたびに手作業でコピーしていたミラー。**

- ミラー側のコミットが本流の**1〜5秒後**に打たれるパターンが94回続く
- セッション ID が本流と完全に同一
- 共通祖先が無いのは、孤児ブランチとして起こして以後ファイルコピーで運用したため
- **07-11 でバックエンドのミラーが停止。** `lib/redis.js` が存在せず、`stripe-webhook.js` は2世代古い。
  動作不能なコードが公開ホストに残っている

---

## 8. 未解決の重要事項

1. **本番はこのリポジトリではない（2026-08-02 確定）。**

   | 項目 | 内容 |
   |---|---|
   | 本番リポジトリ | **`mycomise/mycomise-1p`**（GitHub の所有者が `mycomise` — 別アカウント） |
   | Vercel プロジェクト | `マイコミゼ-1p`（チーム `mycomises-projects`） |
   | デプロイ元ブランチ | **`main`** |
   | 配信ドメイン | `www.mycomise.com` |
   | 最終デプロイ | 2026-07-29（コミット `cd5540e`） |

   `mycomise-1p` は **`api/` と `lib/` がリポジトリ直下**にあり、画面ファイルも同居している。
   だから `mycomise.com/api/verify-code` が成立する。このリポジトリでは `mycomise-api/` という
   フォルダの中にあるためパスが合わず、**ここから本番のAPIは配信されていない。**

   **このリポジトリ（`deepinact6102-netizen/-`）の変更は、本番に一切反映されない。**
   `-` は開発用の別系統。本番へ届けるには `mycomise/mycomise-1p` の `main` に入れる必要がある。

   ⚠️ **1セッションに追加できるのは同一所有者のリポジトリだけ。**
   `-`（`deepinact6102-netizen`）を持つセッションからは `mycomise/mycomise-1p` を追加できない。
   **本番を触るときは `mycomise/mycomise-1p` を最初のソースにした新しいセッションを立てること。**

2. **2026-08-01〜02 の修正5件が本番へ未反映。** 移植内容は次節を参照。

3. **本番の webhook ファイル名にハイフンの取り違えがあった（2026-08-03 判明）。**

   ```
   Stripe が叩く URL : /api/stripe-webhook   ← U+002D（ASCIIのハイフン）
   本番のファイル名   : /api/stripe‐webhook   ← U+2010（別の文字）
   ```

   見た目は同じだが別の文字。**設置以来、Stripe からの配信は全て404で、一度も届いていない。**
   このリポジトリ側のファイル名は純ASCIIで正しい（`od` で確認済み）ため、移植で解消する。

   **連鎖する事実:**
   - `checkout.session.completed` が一度も走っていない
     → **アクセスコードの自動発行もメール送信も、最初から機能していなかった**
   - `sub:{サブスクID} → コード` の対応表が空
     → 修正4（サブスク状態への追随）が効くのは**新規購入分から**
   - Stripe は失敗した配信を数日リトライするため、**リネーム直後に溜まった分が一斉に成功しうる**

   **【重要】実害は無い。** ユーザー確認により、**有料の購入者はまだ一人もいない**
   （オーナー自身の試験購入のみ）。過去の顧客対応は不要。

4. **Stripe のリッスン対象イベントが2件のみ。** 修正4が必要とする
   `customer.subscription.updated` と `invoice.payment_failed` が入っていないため、
   リネームしただけでは発火しない。ダッシュボードでの追加が必要（ユーザー操作）。
2. **既定ブランチが4月のコミットのまま**（ユーザー操作が必要）。
3. **GitHub Pages が有効のまま**（`has_pages: true`）。応急処置済みだが停止が恒久対応。
4. **不要ブランチが残っている。** `gzLdg`（`e83ebab`）と `gjDv3`（`e76293b`）は削除して問題ないが、
   セッションの権限では削除できなかった（403）。GitHub の画面から削除が必要。
   `SoIDX` は既定ブランチのため、設定変更するまで削除できない。
   `mx11T`（`1ee1a53`）は上記の OCR 設計の参照元なので**当面残すこと**。
5. **v5 と demo で本体約2200行が重複している。** 今回のような修正を常に2回書く必要がある。

---

## 9. 参考: 削除・整理したものの SHA

万一戻す必要が出た場合に備えて記録する。

| 対象 | SHA |
|---|---|
| `gzLdg` ブランチ先頭（S2 の完全重複） | `e83ebab` |
| `gjDv3` ブランチ先頭（死んだファイルの翻訳） | `e76293b` |
| `mx11T` ブランチ先頭（**OCR 設計の参照元・保持中**） | `1ee1a53` |
| 死蔵7ファイル削除の直前 | `8cad78d` |
