# プロジェクト運用ルール

## Claude の役割: PM（責任者） ※常時有効・セッションを跨いでも維持

ユーザーからの恒久的な指示。毎セッションの冒頭でこの節を読み、そのまま適用すること。

- Claude は **PM（責任者）** として動く。ユーザーとの窓口を一本化し、部下（サブエージェント）の
  採用・役割分担・統括をすべて自分で判断して行う。
- **重い作業は部下に任せる。** PM 自身は全体把握・意思決定・レビュー・ユーザー対応に専念する。
  PM が直接手を動かしてよいのは、状況確認・軽微な編集・最終判断に必要な範囲まで。
- **部下は原則バックグラウンド起動**（`Agent` ツールを `run_in_background: true`）。
  作業中もユーザーとの会話を止めない。同期実行は結果を待たないと次に進めない場合のみ。
- 継続タスクは新規 Agent を立て直さず `SendMessage` で既存の部下に投げる（文脈を保つため）。
- 部下の報告はユーザーには見えない。**必要な内容は PM が要約して伝える。**
- 部下の報告を鵜呑みにしない。重要な変更は PM が差分・テスト結果を確認してから受け入れる。
- **迷ったら曖昧なまま進めない。** 解釈違いで成果物が変わる分岐は、着手前にユーザーへ確認する。
  一方、確認待ちの間も依存しない作業は進めておく。
- 重要な決定・前提・進行中の作業は忘れないよう、この `CLAUDE.md` に追記して残す。

## リポジトリ

**プロダクト: 焼き鳥店向けの発注・在庫管理ツール（mycomise）。** 4ヶ月・107コミットの実績あり。
単体HTML（インラインJS）で構成され、PWA 対応（`manifest.json` / `sw.js`）。
`mycomise-api/` に Stripe 決済 webhook・アクセスコード認証・Redis 接続があり、
**独自ドメイン mycomise.com はこのプロダクトのもの**と判断される。

主要ファイル: `index.html`（`yakitori_order.html` へリダイレクト）, `yakitori_order.html`,
`yakitori_order_v2.html`, `v2〜v5.html`, `demo.html`（アクセスコード不要の体験版）,
`manual.html`（説明書）, `print.html`（残数記録用紙）, `mycomise-api/`。
※ 版が多数あり、どれが現行かは要確認。

### ブランチ構成（※ main / master は存在しない・PR も Issue も 0件）

| ブランチ | 位置づけ |
|---|---|
| `claude/yakitori-order-table-lvTcH` | **本体。**4月〜7/21 の主力開発（107コミット） |
| `gh-pages` | 公開用。本体よりわずかに古い（6ファイル差分） |
| `claude/pm-role-team-management-ybepo0` | 現行の作業ブランチ。本体の上にリベース済み |
| `yakitori-app-component-SoIDX` 他3本 | 4〜5月の初期作業。役目を終えている |

**注意: 新しいセッションは古いブランチから枝分かれしていることがある。** 着手前に必ず
`git merge-base` で本体との関係を確認し、化石の上で作業していないか検証すること。

## 環境の制約（重要・引っかかりやすい）

- **一般ウェブへの外向き通信は遮断されている。** `WebFetch`/`curl` はプロキシで 403
  (`connect_rejected` / policy denial) になる。GitHub とパッケージレジストリのみ通る。
  組織のポリシー遮断なので迂回しないこと。外部サイトを見る必要がある場合は、
  内容を貼ってもらうか、環境のネットワークポリシー変更をユーザーに依頼する。
- **他セッションの一覧取得・会話閲覧はできない。** セッションは相互に隔離されている。
  他セッションの状況を知る手段は git のブランチ/コミット/PR/Issue のみ。
  他セッションへ指示を送るのは `create_trigger` + セッションID で可能だが片道（返事は読めない）。

## 並列作業の体制

ユーザーの要望は「並列で作業を進めたい」。実現手段は**同一セッション内のサブエージェント**。
PM が全員を直接指揮・監視・受領できる唯一の方法なので、並列化は原則これで行う。

## 未対応の既知の問題（2026-08-01 調査時点・優先度順）

1. **v5 と demo で本体約2200行が完全重複。** 差分は認証まわりの約100行のみ。片方だけ直す
   バグが構造的に起きる。共通部分の外部JS化が根本策。
2. **死蔵ファイル約300KB。** `v2/v3/v4.html`（v2とv4はタイトル2行以外バイト一致）、
   `yakitori_order.html`、`yakitori_order_v2.html`、`yakitori_app.jsx`（無関係な英語版React POS）。
   版が増えたのは Service Worker のキャッシュを飛ばせずファイル名で逃げた経緯によるもの。
3. **`manual.html` の「アプリを開く」が `document.referrer` 依存。** referrer が空だと
   `v5.html` に送られ、デモ利用者が締め出される。`print.html` と同じ `?demo=1` 方式で解消可能。
4. **旧版（`yakitori_order.html`, `v2〜v4.html`）にも同じ認証フェイルオープンが残っている可能性。** 未調査。
5. **`gh-pages` と本体ブランチに共通祖先が無い。** 手作業コピー運用。画面ファイルは同期済みだが
   `mycomise-api/` は旧 `@vercel/kv` 実装のまま1世代古い。**gh-pages には API が無いため
   `/api/verify-code` が必ず404** になる点に注意。
6. **Stripe の Payment Link に `client_reference_id` 等が無く、購入者とコードの紐付けがメールのみ。**
   1コードで台数無制限。`verify-code` にレート制限なし。
7. **Redis のコードに TTL が無い**（解約済みレコードとメールアドレスが永久に残る）。
   `lib/redis.js` にエラーハンドラ無し、`verify-code.js:19` は try/catch の外。
8. **`sw.js` は死んだ旧版2ファイルのみを対象**にしており、現行版は対象外。SW は全現行ページで
   意図的に unregister 済み。ただし `manual.html:282` は「ネット不要」と記載しており実装と乖離。
9. **デッドコード `fixedDeliveryPerDay`**（v5/demo に6箇所）。「レバーは10本固定」のコメントは
   現仕様と矛盾しており誤読を招く。
10. **`print.html:122` の「A4に6枚」は誤り**（実装は9枚）。

### ユーザーに確認して確定した事項（2026-08-01）

- **ホスティングは Vercel。** mycomise.com は Vercel 配信。
- **Stripe の商品はサブスク**（買い切りではない）。
- **GitHub Pages は有効**（API で `has_pages: true` を確認）。リポジトリは public。
  → `https://deepinact6102-netizen.github.io/-/` が生きており、そこには API が無いため
  `/api/verify-code` が404 → 旧コードのフェイルオープンで認証を素通りできる状態だった。
  応急処置として修正済みの画面ファイル4本を `gh-pages` にコピー済み（コミット `5cb2dc3`）。
  **恒久対応はユーザーが Settings → Pages → Source を None にすること。**

### 未確認のまま残っている事項

- `mycomise-api/` のデプロイ方法。現状のままルートをデプロイすると関数パスが
  `/mycomise-api/api/verify-code` になり、アプリ側の `fetch('/api/verify-code')` と一致しない。
  Vercel の Root Directory 設定次第。
- Stripe アカウントの既定 API バージョン。`invoice.subscription` が新旧どちらの形かで
  `stripe-webhook.js` のフォールバックを片方に寄せられる。

### ⚠️ リポジトリ設定の問題（ユーザー操作が必要）

**既定ブランチが `claude/yakitori-app-component-SoIDX`（4月の初日のコミット）になっている。**
新しいセッションはここから始まるため、放置すると毎回「化石の上で作業する」事故が起きる。
`Settings → General → Default branch` を `claude/yakitori-order-table-lvTcH` に変更すべき。

## 決定ログ

- 2026-08-01: PM 体制を開始。上記の運用ルールを恒久ルールとして確定。
- 2026-08-01: 並列化の方式をサブエージェントに決定（複数セッション統括は技術的に不可のため）。
- 2026-08-01: mycomise.com の閲覧を試みたが環境のネットワークポリシーで遮断され断念。
  ただしソースはリポジトリ内（本体ブランチの `mycomise-api/` 他）にあることが判明。
- 2026-08-01: 作業ブランチが4ヶ月前の初期コミットから枝分かれしていたため、
  本体ブランチ `claude/yakitori-order-table-lvTcH` の先頭へリベースして本流に合流（ユーザー判断）。
